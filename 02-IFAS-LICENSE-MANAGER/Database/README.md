# IFAS License Manager - Database

The SQLite database is created automatically when the application starts.

Database file:

`Data/ifas-license-manager.db`

Do not manually create the database file.

The application currently uses:

- Customers
- Licenses
- LicenseFeatures

For production deployment, database backup, migration and access protection should be added.
