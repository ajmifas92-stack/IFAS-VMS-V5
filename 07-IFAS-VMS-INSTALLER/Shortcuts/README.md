# Shortcuts

The bootstrap installer creates a Start Menu shortcut for the VMS Client when the executable exists.

Desktop shortcuts are intentionally not forced by default. They can be added later to the final signed installer based on deployment requirements.
