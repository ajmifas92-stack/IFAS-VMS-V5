# 08-IFAS-VMS-UPDATER

Update foundation for IFAS VMS Windows deployments.

Includes version manifests, SHA-256 package verification, staging, backup, rollback, and PowerShell update scripts.

Production releases should use signed update packages, HTTPS, authenticated metadata, and signature verification.
