# IFAS VMS Visual Assets

Application icons for Client, Server, Config, License Manager, Log Viewer and Updater.
These are packaged as application assets; Windows Services themselves are represented by service names/status in Services.msc rather than a user-facing desktop icon.
